{
  "name": "health-companion-domain-models",
  "version": "0.1.0",
  "description": "Shared domain models for Health Companion",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "rimraf": "^5.0.0"
  }
}
